# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 9

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec "rofi -combi-modi drun,run,window,ssh -show combi -modi combi &"
bindsym $mod+p exec "rofi -show window -modi window &"
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+g split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+Shift+a focus child

# switch to workspace
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10
bindsym $mod+Ctrl+1 workspace number 11
bindsym $mod+Ctrl+2 workspace number 12
bindsym $mod+Ctrl+3 workspace number 13
bindsym $mod+Ctrl+4 workspace number 14
bindsym $mod+Ctrl+5 workspace number 15
bindsym $mod+Ctrl+6 workspace number 16
bindsym $mod+Ctrl+7 workspace number 17
bindsym $mod+Ctrl+8 workspace number 18
bindsym $mod+Ctrl+9 workspace number 19
bindsym $mod+Ctrl+0 workspace number 20

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10
bindsym $mod+Shift+Ctrl+1 move container to workspace number 11
bindsym $mod+Shift+Ctrl+2 move container to workspace number 12
bindsym $mod+Shift+Ctrl+3 move container to workspace number 13
bindsym $mod+Shift+Ctrl+4 move container to workspace number 14
bindsym $mod+Shift+Ctrl+5 move container to workspace number 15
bindsym $mod+Shift+Ctrl+6 move container to workspace number 16
bindsym $mod+Shift+Ctrl+7 move container to workspace number 17
bindsym $mod+Shift+Ctrl+8 move container to workspace number 18
bindsym $mod+Shift+Ctrl+9 move container to workspace number 19
bindsym $mod+Shift+Ctrl+0 move container to workspace number 20

# move workspaces between outputs
bindsym $mod+m move workspace to output right
bindsym $mod+Shift+m move workspace to output left

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

mode "workspace" {
        # These bindings trigger as soon as you enter the workspace mode

        # switch to workspace
        bindsym $mod+1 workspace number 1; mode "default"
        bindsym $mod+2 workspace number 2; mode "default"
        bindsym $mod+3 workspace number 3; mode "default"
        bindsym $mod+4 workspace number 4; mode "default"
        bindsym $mod+5 workspace number 5; mode "default"
        bindsym $mod+6 workspace number 6; mode "default"
        bindsym $mod+7 workspace number 7; mode "default"
        bindsym $mod+8 workspace number 8; mode "default"
        bindsym $mod+9 workspace number 9; mode "default"
        bindsym $mod+0 workspace number 10; mode "default"
        bindsym $mod+Ctrl+1 workspace number 11; mode "default"
        bindsym $mod+Ctrl+2 workspace number 12; mode "default"
        bindsym $mod+Ctrl+3 workspace number 13; mode "default"
        bindsym $mod+Ctrl+4 workspace number 14; mode "default"
        bindsym $mod+Ctrl+5 workspace number 15; mode "default"
        bindsym $mod+Ctrl+6 workspace number 16; mode "default"
        bindsym $mod+Ctrl+7 workspace number 17; mode "default"
        bindsym $mod+Ctrl+8 workspace number 18; mode "default"
        bindsym $mod+Ctrl+9 workspace number 19; mode "default"
        bindsym $mod+Ctrl+0 workspace number 20; mode "default"
        bindsym $mod+a workspace a; mode "default"
        bindsym $mod+b workspace b; mode "default"
        bindsym $mod+c workspace c; mode "default"
        bindsym $mod+d workspace d; mode "default"
        bindsym $mod+e workspace e; mode "default"
        bindsym $mod+f workspace f; mode "default"
        bindsym $mod+g workspace g; mode "default"
        bindsym $mod+h workspace h; mode "default"
        bindsym $mod+i workspace i; mode "default"
        bindsym $mod+j workspace j; mode "default"
        bindsym $mod+k workspace k; mode "default"
        bindsym $mod+l workspace l; mode "default"
        bindsym $mod+m workspace m; mode "default"
        bindsym $mod+n workspace n; mode "default"
        bindsym $mod+o workspace o; mode "default"
        bindsym $mod+p workspace p; mode "default"
        bindsym $mod+q workspace q; mode "default"
        bindsym $mod+r workspace r; mode "default"
        bindsym $mod+s workspace s; mode "default"
        bindsym $mod+t workspace t; mode "default"
        bindsym $mod+u workspace u; mode "default"
        bindsym $mod+v workspace v; mode "default"
        bindsym $mod+w workspace w; mode "default"
        bindsym $mod+x workspace x; mode "default"
        bindsym $mod+y workspace y; mode "default"
        bindsym $mod+z workspace z; mode "default"
        bindsym $mod+Ctrl+a workspace A; mode "default"
        bindsym $mod+Ctrl+b workspace B; mode "default"
        bindsym $mod+Ctrl+c workspace C; mode "default"
        bindsym $mod+Ctrl+d workspace D; mode "default"
        bindsym $mod+Ctrl+e workspace E; mode "default"
        bindsym $mod+Ctrl+f workspace F; mode "default"
        bindsym $mod+Ctrl+g workspace G; mode "default"
        bindsym $mod+Ctrl+h workspace H; mode "default"
        bindsym $mod+Ctrl+i workspace I; mode "default"
        bindsym $mod+Ctrl+j workspace J; mode "default"
        bindsym $mod+Ctrl+k workspace K; mode "default"
        bindsym $mod+Ctrl+l workspace L; mode "default"
        bindsym $mod+Ctrl+m workspace M; mode "default"
        bindsym $mod+Ctrl+n workspace N; mode "default"
        bindsym $mod+Ctrl+o workspace O; mode "default"
        bindsym $mod+Ctrl+p workspace P; mode "default"
        bindsym $mod+Ctrl+q workspace Q; mode "default"
        bindsym $mod+Ctrl+r workspace R; mode "default"
        bindsym $mod+Ctrl+s workspace S; mode "default"
        bindsym $mod+Ctrl+t workspace T; mode "default"
        bindsym $mod+Ctrl+u workspace U; mode "default"
        bindsym $mod+Ctrl+v workspace V; mode "default"
        bindsym $mod+Ctrl+w workspace W; mode "default"
        bindsym $mod+Ctrl+x workspace X; mode "default"
        bindsym $mod+Ctrl+y workspace Y; mode "default"
        bindsym $mod+Ctrl+z workspace Z; mode "default"

        # move focused container to workspace
        bindsym $mod+Shift+1 move container to workspace number 1; mode "default"
        bindsym $mod+Shift+2 move container to workspace number 2; mode "default"
        bindsym $mod+Shift+3 move container to workspace number 3; mode "default"
        bindsym $mod+Shift+4 move container to workspace number 4; mode "default"
        bindsym $mod+Shift+5 move container to workspace number 5; mode "default"
        bindsym $mod+Shift+6 move container to workspace number 6; mode "default"
        bindsym $mod+Shift+7 move container to workspace number 7; mode "default"
        bindsym $mod+Shift+8 move container to workspace number 8; mode "default"
        bindsym $mod+Shift+9 move container to workspace number 9; mode "default"
        bindsym $mod+Shift+0 move container to workspace number 10; mode "default"
        bindsym $mod+Shift+Ctrl+1 move container to workspace number 11; mode "default"
        bindsym $mod+Shift+Ctrl+2 move container to workspace number 12; mode "default"
        bindsym $mod+Shift+Ctrl+3 move container to workspace number 13; mode "default"
        bindsym $mod+Shift+Ctrl+4 move container to workspace number 14; mode "default"
        bindsym $mod+Shift+Ctrl+5 move container to workspace number 15; mode "default"
        bindsym $mod+Shift+Ctrl+6 move container to workspace number 16; mode "default"
        bindsym $mod+Shift+Ctrl+7 move container to workspace number 17; mode "default"
        bindsym $mod+Shift+Ctrl+8 move container to workspace number 18; mode "default"
        bindsym $mod+Shift+Ctrl+9 move container to workspace number 19; mode "default"
        bindsym $mod+Shift+Ctrl+0 move container to workspace number 20; mode "default"
        bindsym $mod+Shift+a move container to workspace a; mode "default"
        bindsym $mod+Shift+b move container to workspace b; mode "default"
        bindsym $mod+Shift+c move container to workspace c; mode "default"
        bindsym $mod+Shift+d move container to workspace d; mode "default"
        bindsym $mod+Shift+e move container to workspace e; mode "default"
        bindsym $mod+Shift+f move container to workspace f; mode "default"
        bindsym $mod+Shift+g move container to workspace g; mode "default"
        bindsym $mod+Shift+h move container to workspace h; mode "default"
        bindsym $mod+Shift+i move container to workspace i; mode "default"
        bindsym $mod+Shift+j move container to workspace j; mode "default"
        bindsym $mod+Shift+k move jontainer to workspace k; mode "default"
        bindsym $mod+Shift+l move container to workspace l; mode "default"
        bindsym $mod+Shift+m move container to workspace m; mode "default"
        bindsym $mod+Shift+n move container to workspace n; mode "default"
        bindsym $mod+Shift+o move container to workspace o; mode "default"
        bindsym $mod+Shift+p move container to workspace p; mode "default"
        bindsym $mod+Shift+q move container to workspace q; mode "default"
        bindsym $mod+Shift+r move container to workspace r; mode "default"
        bindsym $mod+Shift+s move container to workspace s; mode "default"
        bindsym $mod+Shift+t move container to workspace t; mode "default"
        bindsym $mod+Shift+u move container to workspace u; mode "default"
        bindsym $mod+Shift+v move container to workspace v; mode "default"
        bindsym $mod+Shift+w move container to workspace w; mode "default"
        bindsym $mod+Shift+x move container to workspace x; mode "default"
        bindsym $mod+Shift+y move container to workspace y; mode "default"
        bindsym $mod+Shift+z move container to workspace z; mode "default"
        bindsym $mod+Shift+Ctrl+a move container to workspace A; mode "default"
        bindsym $mod+Shift+Ctrl+b move container to workspace B; mode "default"
        bindsym $mod+Shift+Ctrl+c move container to workspace C; mode "default"
        bindsym $mod+Shift+Ctrl+d move container to workspace D; mode "default"
        bindsym $mod+Shift+Ctrl+e move container to workspace E; mode "default"
        bindsym $mod+Shift+Ctrl+f move container to workspace F; mode "default"
        bindsym $mod+Shift+Ctrl+g move container to workspace G; mode "default"
        bindsym $mod+Shift+Ctrl+h move container to workspace H; mode "default"
        bindsym $mod+Shift+Ctrl+i move container to workspace I; mode "default"
        bindsym $mod+Shift+Ctrl+j move container to workspace J; mode "default"
        bindsym $mod+Shift+Ctrl+k move container to workspace K; mode "default"
        bindsym $mod+Shift+Ctrl+l move container to workspace L; mode "default"
        bindsym $mod+Shift+Ctrl+m move container to workspace M; mode "default"
        bindsym $mod+Shift+Ctrl+n move container to workspace N; mode "default"
        bindsym $mod+Shift+Ctrl+o move container to workspace O; mode "default"
        bindsym $mod+Shift+Ctrl+p move container to workspace P; mode "default"
        bindsym $mod+Shift+Ctrl+q move container to workspace Q; mode "default"
        bindsym $mod+Shift+Ctrl+r move container to workspace R; mode "default"
        bindsym $mod+Shift+Ctrl+s move container to workspace S; mode "default"
        bindsym $mod+Shift+Ctrl+t move container to workspace T; mode "default"
        bindsym $mod+Shift+Ctrl+u move container to workspace U; mode "default"
        bindsym $mod+Shift+Ctrl+v move container to workspace V; mode "default"
        bindsym $mod+Shift+Ctrl+w move container to workspace W; mode "default"
        bindsym $mod+Shift+Ctrl+x move container to workspace X; mode "default"
        bindsym $mod+Shift+Ctrl+y move container to workspace Y; mode "default"
        bindsym $mod+Shift+Ctrl+z move container to workspace Z; mode "default"

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+z mode "workspace"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3blocks
        position top
        tray_output primary
        colors {
                background #002b36
                separator  #073642
                statusline #93a1a1
        }
}

# class                 border  backgr. text    indicator
client.focused          #002b36 #0000ff #fdf6e3 #cb4b16
client.focused_inactive #002b36 #657b83 #fdf6e3 #657b83
client.unfocused        #002b36 #00141a #586e75 #000000
client.urgent           #002b36 #268bd2 #fdf6e3 #268bd2
client.placeholder      #002b36 #000000 #073642 #000000

focus_follows_mouse no
focus_on_window_activation focus
bindsym $mod+u [urgent="latest"] focus

new_window pixel 3
bindsym $mod+b border normal 3
bindsym $mod+Shift+b border pixel 3

show_marks yes

bindsym $mod+y sticky toggle

bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show
bindsym $mod+x scratchpad show

bindsym $mod+BackSpace workspace back_and_forth

bindsym $mod+colon exec i3-input

exec --no-startup-id xrdb -merge ~/.Xresources &
exec --no-startup-id compton --no-fading-openclose &
exec --no-startup-id feh --bg-fill --randomize ~/.wallpaper/* &
exec --no-startup-id dunst &
exec --no-startup-id nm-applet &
exec --no-startup-id udiskie &
